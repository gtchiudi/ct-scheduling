[pytest]
DJANGO_SETTINGS_MODULE = server.settings
pythonpath = backend
addopts = -v --tb=short
testpaths = backend/members/tests

markers =
    e2e: marks tests as end-to-end (run against a live app instance)
